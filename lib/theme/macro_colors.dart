import 'package:flutter/material.dart';

/// Consistent macro colours across charts and labels (food catalog & nutrition UI).
abstract final class MacroColors {
  /// Neon-friendly macro accents for dark UI charts and chips.
  static const Color protein = Color(0xFFFF8A65);
  static const Color carbs = Color(0xFF69F0AE);
  static const Color fat = Color(0xFFE040FB);
}
